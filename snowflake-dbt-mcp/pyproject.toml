[project]
name = "snowflake-dbt-mcp"
version = "0.1.0"
description = "MCP server exposing dbt + Snowflake development tools to Claude"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]
