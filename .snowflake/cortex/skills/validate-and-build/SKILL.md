---
name: validate-and-build
description: "DEPRECATED — duplicate of running-dbt-commands. Use running-dbt-commands instead."
user-invocable: false
metadata:
  author: snowflake-dbt-starter-kit
  deprecated: true
  replaced-by: running-dbt-commands
---

# DEPRECATED — Use `running-dbt-commands` instead

The `running-dbt-commands` skill covers compile, build, show, and run_results analysis — all the same steps in this workflow.
